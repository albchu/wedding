import React from "react"
import "./FramedContainer.css"

const FramedContainer = ({ children }) => {
  return <div className="FramedContainer_container">{children}</div>
}

export default FramedContainer
